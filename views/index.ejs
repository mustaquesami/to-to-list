    <%- include ("layout/header") %>
    <main>
        <form action="/" method="post">
            <input type="text" name="plName" placeholder="Add Programming Language">
            <button type="submit">Add Language</button>
        </form>
        <ul>
            <% if(plNameKey.length>0){ %>
                <% for(let i=0; i<plNameKey.length; i++){ %>
                    <li><%= plNameKey[i] %></li>
                <%}%>
            <% }else{ %>
                <p>No Data Found</p>
            <% } %>
            
        </ul>
    </main>
    <%- include ("layout/footer") %>